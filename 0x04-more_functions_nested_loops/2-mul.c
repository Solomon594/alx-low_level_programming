#include "main.h"

/**
 * mul - multiplies twon integers
 * @a: the first number to multiply
 * @b: the second number
 * Return: the multiplication results
 */
int mul(int a, int b)
{
	return (a * b);
}
